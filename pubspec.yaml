name: mobx_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  mobx: 
  flutter_mobx: 
  provider:  

dev_dependencies:
  mobx_codegen:
  build_runner: # flutter pub run build_runner build --- flutter pub run build_runner watch
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true